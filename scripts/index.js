'use strict';
/* global shoppingList, cuid */

// eslint-disable-next-line no-unused-vars
function main() {
  shoppingList.bindEventListeners();
  shoppingList.render();
}
console.log(Item);
console.log(foo1);
console.log(store);
console.log(foo);

$(main);

